name: Test

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: echo "hello"
      - name: hello user
        run: echo "hello $USER"
  
  details:
    runs-on: ubuntu-latest
    steps:
      - name: details
        run: docker --version
