Name: Test

On:
  workflow_dispatch:
  pull_request:
    branch: [main]
Jobs:
  greet:
    run-on: ubuntu-latest
    steps:
      - name: hello
        run: echo "hello"
      - name: hello user
        run: echo "hello $USER"
  
  details:
    run-on: ubuntu-latest
    steps:
      - name: details
        run: docker --version
